import { IJetURL, IJetView, IPath, IRoute, IJetURLChunk } from "./interfaces";
export declare class Route implements IRoute {
    route: IPath;
    private index;
    private _next;
    constructor(route: string | IPath, index: number);
    current(): IJetURLChunk;
    next(): IJetURLChunk;
    suburl(): IJetURL;
    shift(): IRoute;
    refresh(): void;
    toString(): string;
    _join(path: string, kids?: boolean): IJetURLChunk[];
    append(path: string): string;
    show(path: string, view: IJetView, kids?: boolean): Promise<void>;
    size(n: number): void;
    split(): IRoute;
    update(name: string, value: string, index?: number): any;
}
